package gui.listeners;
/**
 * Atualização da lista quando os dados mudarem
 * @author migue
 *
 */
public interface DataChangeListener {
	/*
	 * Evento para ser disparado quando os dados mudarem
	 */
	void onDataChanged();
}
